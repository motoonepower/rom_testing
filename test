#!/bin/bash
sudo apt update
sudo apt upgrade
sudo apt install gpg
df -h
free -h
ls
nproc
mkdir -p /tmp/rom # Where to sync source
mkdir -p /tmp/ccache # Where to sync source
cd /tmp/rom
df -h
free -h
